{
    "name": "program-platform-starter",
    "version": "1.0.0",
    "description": "Reusable cohort learning platform starter",
    "private": true,
    "scripts": {
        "clean": "rm -rf client/dist server/dist server/client-dist",
        "dev:client": "cd client && npm run dev",
        "dev:server": "cd server && npm run start:dev",
        "build:client": "cd client && npm run build",
        "build:server": "cd server && npm run build && cp -r ../client/dist ./client-dist && cp -r ./client-dist ./dist/client-dist",
        "build": "npm run clean && npm run build:client && npm run build:server"
    }
}
